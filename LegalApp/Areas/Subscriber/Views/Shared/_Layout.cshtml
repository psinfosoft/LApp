<!DOCTYPE html>
<html>
<head>
    @{
        long UserID = LegalApp.Common.UtilityClass.UserID();
        string UserName = LegalApp.Common.UtilityClass.UserName();
    }

    <title>@ViewBag.Title - Legal App</title>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/KendoAllJs")
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/KendoAllCss")
   
    <script type="text/javascript">
        function notify(message, type) {
            var xoffSet;
            var alignment;
            if (message.length >= 60) {
                xoffSet = 350;
                alignment = "left";
            }
            else {
                xoffSet = 0;
                alignment = "center";
            }
            $.growl({
                message: message
            }, {
                type: type,
                allow_dismiss: true,
                label: 'Cancel',
                className: 'btn-xs btn-inverse',
                placement: {
                    from: 'top',
                    align: alignment
                },
                offset: {
                    x: xoffSet,
                    y: 50
                },
                delay: 5000,
                animate: {
                    enter: 'animated bounceIn',
                    exit: 'animated bounceOut'
                }
            });
        }
    </script>
    <meta charset="utf-8">
   
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Google Font -->
    <link href='https://fonts.googleapis.com/css?family=Montserrat:400,700' rel='stylesheet' type='text/css' />
    <!-- Font Awesome -->
    <link href='https://maxcdn.bootstrapcdn.com/font-awesome/4.6.2/css/font-awesome.min.css' rel='stylesheet' type='text/css' />
    <!-- ionicons -->
    <link href='https://cdnjs.cloudflare.com/ajax/libs/ionicons/2.0.1/css/ionicons.css' rel='stylesheet' type='text/css' />
</head>
<body>
    <div>
        <nav id="global-nav" class="navbar navbar-default navbar-static-top navbar-inverse">
            <div class="container-fluid">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a href="@Url.Action("Index","Dashboard")" class="navbar-brand">Legal App</a>
                </div>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        @*<li id="aProjects"><a href="@Url.Action("Index","Projects")">Projects</a></li>
                        <li id="aVerifications"><a href="@Url.Action("Index","Verifications")">Verifications</a></li>
                        @if (UserType == 1)
                        {
                            <li id="aClients"><a href="@Url.Action("Index", "Client")">Clients</a></li>
                        }*@
                        @*<li><a href="@Url.Action("Index","Project")" class="btn btn-default">Create Project</a></li>*@
                    </ul>

                    <ul class="nav navbar-nav navbar-right">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="@Url.Action("Index", "Profile")">User Profile</a></li>
                                <li><a href="@Url.Action("LogOut", "Login")">Log Out</a></li>
                                @*@if (UserType == 1)
                                {
                                    <li class="divider"></li>
                                    <li class="dropdown-header">Application Settings</li>
                                    <li><a href="@Url.Action("Index", "UserManagement")">User Management</a></li>
                                    <li><a href="@Url.Action("Index", "EventVerificationType")">Verification Event Types</a></li>
                                }*@
                            </ul>
                        </li>
                    </ul>
                </div>
                <!-- /.navbar-collapse -->
            </div>
            <!-- /.container-fluid -->
        </nav>
    </div>
    <div class="wrapper-main">
        <div class="container-fluid">
            @RenderBody()
        </div>
    </div>
    <footer class="footer">
        <div class="container">
            <p class="text-muted"><a>test@elevateservices.com</a></p>
        </div>
    </footer>
    @*<div id="loading" class="hide">
        <div id="loading-content">
            Loading...
        </div>
    </div>*@
    <style>
        #loading {
            width: 100%;
            height: 100%;
            top: 0px;
            left: 0px;
            position: fixed;
            display: block;
            opacity: 0.7;
            background-color: #fff;
            z-index: 99;
            text-align: center;
        }

        #loading-content {
            position: absolute;
            top: 50%;
            left: 50%;
            text-align: center;
            z-index: 100;
        }

        .hide {
            display: none;
        }
    </style>
</body>


</html>
